# 🔤 字体快速设置

## 获得最佳显示效果

为了在终端模拟器中看到漂亮的文件图标和符号，请按照以下步骤设置字体：

### 🚀 一键设置（推荐）

```bash
# 运行字体下载脚本
./scripts/download_fonts.sh

# 重新启动应用
flutter run
```

### 📱 手动设置

1. **下载字体**
   - 访问: https://github.com/ryanoasis/nerd-fonts/releases
   - 下载 `JetBrainsMono.zip`

2. **安装字体**
   ```bash
   # 解压字体文件
   unzip JetBrainsMono.zip
   
   # 复制字体到项目
   cp JetBrainsMonoNerdFont-Regular.ttf assets/fonts/
   cp JetBrainsMonoNerdFont-Bold.ttf assets/fonts/
   ```

3. **重新运行应用**
   ```bash
   flutter pub get
   flutter run
   ```

## 🎨 显示效果对比

### 使用 Nerd Font 时
```
 📁 documents/
  main.dart (2.5KB)
  config.json (1.2KB)
  README.md (3.8KB)
```

### 使用系统字体时
```
[DIR] documents/
[DART] main.dart (2.5KB)
[JSON] config.json (1.2KB)
[MD] README.md (3.8KB)
```

## ⚙️ 字体设置

在应用中输入 `settings` 命令可以：
- 选择字体系列
- 调整字体大小
- 更改颜色主题
- 预览字体效果

## 🔧 故障排除

### 字体不显示图标
- 确认字体文件已正确放置在 `assets/fonts/` 目录
- 重新运行 `flutter pub get`
- 重启应用

### 中文显示异常
- 确保系统安装了中文字体
- 在设置中尝试不同的字体选项

---

**提示**: 即使没有安装 Nerd Font，应用也能正常工作，只是会使用文本标识符代替图标。